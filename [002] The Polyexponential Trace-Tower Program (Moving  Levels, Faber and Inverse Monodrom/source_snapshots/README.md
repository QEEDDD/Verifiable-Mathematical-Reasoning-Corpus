# Predecessor source snapshots

This directory preserves the two principal predecessor sources used during
integration:

- `KEIO_Moving_Level_Trace_Towers_v2_3.tex`
- `KEIO_Trace_Barrier_Escape_Addendum_v2_4.tex`

They are included for provenance only and contain superseded organization and
wording.  Do not compile or concatenate them to reproduce the current release.  The
release authority is `main.tex` together with `parts/`.
